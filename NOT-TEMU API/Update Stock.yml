info:
  name: Update Stock
  type: http
  seq: 8

http:
  method: PATCH
  url: http://localhost:8081/inventory/4
  body:
    type: json
    data: |-
      {
        "quantity": 50
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
